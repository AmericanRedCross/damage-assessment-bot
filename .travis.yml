language: node_js
node_js: 
  - "lts/*"
before_install:
  - rm -rf ".git"
  - cd api
install:
  - npm install
  - npm run build:dev
script: echo "Skipping NPM Tests!"
cache:
  directories:
    - "node_modules"
before_deploy:
  - cd dist
  - git init
deploy:
  provider: azure_web_apps
  on:
    branch: Travis-CI-Setup