{
    "scripts": {
        "start": "node ./deploy.js",
        "start:dev": "cross-env DeployEnvironment=dev npm run start",
        "start:stage": "cross-env DeployEnvironment=stage npm run start",
        "start:prod": "cross-env DeployEnvironment=prod npm run start"
    },
    "dependencies": {
        "@azure/cosmos": "^2.0.1",
        "adm-zip": "^0.4.11",
        "axios": "^0.18.0",
        "crypto": "^1.0.1",
        "glob": "^7.1.3",
        "jsonwebtoken": "^8.3.0"
    },
    "devDependencies": {
        "cross-env": "^5.2.0"
    }
}
