{
    "scripts": {
        "install": "npm run install-api && npm run install-common && npm run install-web && npm run install-deploy",
        "install-api": "cd api && npm install",
        "install-common": "cd common && npm install",
        "install-web": "cd web && npm install",
        "install-deploy": "cd deploy && npm install",
        "deploy": "npm run build --prefix web && npm run build --prefix api && npm start --prefix deploy"
    },
    "devDependencies": {
        "axios": "^0.18.0"
    },
    "dependencies": {
        "myanmar-tools": "^1.1.1"
    }
}
